# Database
DATABASE_URL=postgres://penca:penca@db:5432/penca_development

# Test database — dev and test MUST be separate databases. Optional: if unset,
# config/database.yml derives it from DATABASE_URL by swapping the DB name for
# penca_test. Set it explicitly only to point the suite somewhere else.
# TEST_DATABASE_URL=postgres://penca:penca@db:5432/penca_test

# Rails
RAILS_ENV=development
SECRET_KEY_BASE=replace_me_with_rails_secret
RAILS_MASTER_KEY=replace_me

# Admin allowlist (comma-separated emails that auto-promote to admin)
ADMIN_EMAILS=admin@example.com

# Google OAuth (from Google Cloud Console — see "Google OAuth setup" in README.md)
# Leave blank to run without Google sign-in; the app still boots.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# football-data.org
FOOTBALL_DATA_API_KEY=
FOOTBALL_DATA_BASE_URL=https://api.football-data.org/v4

# Resend (transactional email)
RESEND_API_KEY=
MAILER_FROM=Penca Mundial <no-reply@yourdomain.com>

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,https://your-frontend.vercel.app

# Frontend base URL (used in email links)
FRONTEND_URL=http://localhost:5173

# Run solid_queue inside Puma instead of a separate worker (free tier friendly)
SOLID_QUEUE_IN_PUMA=true
