#!/bin/bash -e

# When starting the Rails server (directly or via Thruster, with or without
# extra flags), create/migrate the database first.
if [[ "$*" == *"rails"*"server"* ]]; then
  ./bin/rails db:prepare
fi

exec "${@}"
